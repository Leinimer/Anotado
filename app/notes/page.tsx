import { MainLayout } from '@/src/features/core_layout/ui/MainLayout';

export const metadata = {
  title: 'Notas — anotado!',
  description: 'Suas notas e cadernos organizados com elegância.',
};

export default function NotesPage() {
  return <MainLayout />;
}
